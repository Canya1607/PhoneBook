import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  avatar: {
    alignItems: 'center',
    marginVertical: 16,
  },
});

export default styles;
